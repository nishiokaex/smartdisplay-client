import 'package:flutter/material.dart';

class ViewerRoot extends StatelessWidget {
  ViewerRoot({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("ViewerRoot"),
      ),
    );
  }
}
